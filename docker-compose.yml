version: "3.8"

services:
  pennybot:
    build: .
    container_name: pennybot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./pennybot.log:/app/pennybot.log
    environment:
      - DB_PATH=data/alerts_history.db
